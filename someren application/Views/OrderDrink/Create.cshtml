@model someren_application.Models.OrderDrinks

@{
    ViewData["Title"] = "Order Drinks";
}

<form method="post" asp-controller="OrderDrink" asp-action="Create" onsubmit="return confirm('Are you sure you want to submit this order?');">
    <div class="container mt-5">
        <div class="border p-5 shadow-lg rounded" style="min-height: 500px;">
            <div class="row pb-4">
                <h2 class="text-primary text-center">
                    Create Order
                </h2>
                <hr />
            </div>

            @if (TempData["ErrorMessage"] != null)
            {
                <div class="alert alert-danger">
                    @TempData["ErrorMessage"]
                </div>
            }

            <div class="mb-4 row">
                <div class="col-md-4">
                    <label for="studentSelect" class="form-label">Student Details</label>
                    @if (Model.Students.Any())
                    {
                        <select asp-for="Students[0].StudentId" class="form-select hover-animate" id="studentSelect">
                            <option value="">Select a Student</option>
                            @foreach (var student in Model.Students)
                            {
                                <option value="@student.StudentId">@student.StudentNumber - @student.LastName - @student.FirstName - @student.Rooms[0].RoomNumber</option>
                            }
                        </select>
                    }
                    else
                    {
                        <p>No students available. Please add students first.</p>
                    }
                    <span asp-validation-for="Students[0].StudentId" class="text-danger"></span>
                </div>
                <div class="col-md-4">
                    <label for="drinkSelect" class="form-label">Drink Details</label>
                    @if (Model.Drinks.Any())
                    {
                        <select asp-for="Drinks[0].DrinkId" class="form-select hover-animate" id="drinkSelect">
                            <option value="">Select a Drink</option>
                            @foreach (var drink in Model.Drinks)
                            {
                                <option value="@drink.DrinkId">@drink.DrinkName - @drink.IsAlcoholic - @drink.VatRate - @drink.Quantity</option>
                            }
                        </select>
                    }
                    else
                    {
                        <p>No drinks available. Please add drinks first.</p>
                    }
                    <span asp-validation-for="Drinks[0].DrinkId" class="text-danger"></span>
                </div>
                <div class="col-md-4">
                    <label for="TotalDrink" class="form-label">Total Drink</label>
                    <input asp-for="TotalDrink" class="form-control hover-animate" id="TotalDrink" />
                    <span asp-validation-for="TotalDrink" class="text-danger"></span>
                </div>
            </div>
            <div class="mb-4 row justify-content-center">
                <div class="col-md-3">
                    <button type="submit" class="btn btn-primary w-100 hover-animate">Submit Order</button>
                </div>
                <div class="col-md-3 mt-2 mt-md-0">
                    <a asp-controller="OrderDrink" asp-action="Index" class="btn btn-outline-secondary w-100 hover-animate">
                        Back To View Orders
                    </a>
                </div>
            </div>
        </div>
    </div>
</form>


<style>
    /* Hover effect for input and select elements */
    .hover-animate {
        transition: all 0.3s ease;
    }

        .hover-animate:hover {
            border-color: #007bff; /* Change border color on hover */
            box-shadow: 0 0 5px rgba(0, 123, 255, 0.5); /* Add a subtle shadow */
        }

    /* Hover effect for buttons */
    .btn-primary:hover {
        background-color: #0056b3; /* Darken the primary button on hover */
        border-color: #0056b3;
    }

    .btn-outline-secondary:hover {
        background-color: #f8f9fa; /* Lighten the secondary button on hover */
        color: #0056b3;
        border-color: #0056b3;
    }

    /* Hover effect for the links */
    .btn-outline-secondary:hover {
        text-decoration: none;
    }
</style>
